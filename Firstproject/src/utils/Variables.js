import {Dimensions} from 'react-native';
const {height, width} = Dimensions.get('window');

const colors = {
  white: '#fff',
  black: '#000',
  gray: 'gray',
};

const fontSizes = {
  smallest: 8,
  smaller: 10,
  small: 12,
  medium: 14,
  large: 16,
  xlarge: 18,
  xxlarge: 20,
};

export {colors, fontSizes};
